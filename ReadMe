NoteBoard-CRUD-App

Description
-----------
This App was made in Java 17 and the Spring MVC Web framework with SQLite in-memory database. Dependencies are downloaded from Maven Repository.
It allows users to quickly and easily add, delete, and update and review list of board records. 

Project Build and Run
---------------------
After download the source, it can compile and run upon either Maven or SpringBootApplication, Run as Java Application from BoardingManagementWebApplication.java file.
When a web server is up, access the app through localhost:8080 by using username -> sa and password -> sa. 
Login username and password can alter on application.properties file.


 










